import * as React from "react";
import { HomeIcon, AuthorIcon } from "../../icons";

const bottomNavMenu = [
  { title: "Top", path: "/", icon: <HomeIcon /> },
  { title: "Author", path: "/author/", icon: <AuthorIcon /> },
];

export default bottomNavMenu;
