// import { graphql, useStaticQuery } from 'gatsby';

function useFragments() {
  /*
  useStaticQuery(graphql`
    fragment MdxList on Mdx {
      id
      slug
      frontmatter {
        title
        date(formatString: "YYYY-MM-DD")
        author {
          name
          slug
        }
      }
    }
  `);
  */
}

export default useFragments;
